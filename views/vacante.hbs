<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="UTF-8">
	<link href="https://fonts.googleapis.com/css?family=Rubik:400,700,900" rel="stylesheet">
	<link rel="stylesheet" type="text/css" href="/css/app.css">
	<title>{{nombrePagina}}</title>
</head>
<body>

	<div class="nombre-sitio contenedor">
		<h1>
			<a href="/">devJobs</a>
		</h1>

		<div class="buscador">
			<form action="#">
				<input type="text" class="buscar">
				<input type="submit" value="buscar">
			</form>
		</div>
	</div>

	<header class="site-header contenedor separador">
		<h2>{{nombrePagina}}</h2>
	</header>

	<div class="contenido-superior vacante contenedor">
		<div class="caja">
			<p class="etiqueta">Empresa</p>
			<p class="etiqueta">{{vacante.empresa}}</p>
		</div>

		<div class="caja">
			<p class="etiqueta">Ubicacion</p>
			<p class="etiqueta">{{vacante.ubicacion}}</p>
		</div>

		<div class="caja">
			<p class="etiqueta">Contrato</p>
			<p class="etiqueta">{{vacante.contrato}}</p>
		</div>

		<div class="caja">
			<p class="etiqueta">Salario</p>
			<p class="etiqueta">$ {{vacante.salario}}</p>
		</div>
	</div>

	<div class="vacante-contenedor contenedor">
			<main class="contenido">
				<h2>Descripcion del puesto</h2>
				<div class="vacante-descripcion">
					{{{vacante.descripcion}}}
				</div>

				<a href="/vacantes/editar/{{vacante.url}}" class="btn btn-azul editar-btn">Editar vacante</a>
			</main>

			<aside class="sidebar">
				<h2>Conociemntos deseados</h2>
				<ul class="skills">
					{{#each vacante.skills}}
						<li>{{this}}</li>
					{{/each}}
				</ul>
			</aside>
	</div>

	<div class="contenedor datos-reclutador">
		<div class="enviar-datos">
			<h2>Contactar al reclutador</h2>
			<p>Llena el siguiente formulario, sube el curriculim y pronto te contactaremos</p>
			<form action="/vacantes/{{vacante.url}}" method="POST" class="default-form" enctype="multipart/form-data">
				<div class="campo">
					<label>Nombre</label>
					<input type="text" name="nombre" placeholder="Tu nombre" />
				</div>

				<div class="campo">
					<label>E-mail</label>
					<input type="email" name="email" placeholder="Tu email" />
				</div>

				<div class="campo">
					<label>CV (PDF)</label>
					<input type="file" name="cv" />
				</div>

				<div class="campo">
					<label>Nombre</label>
					<input type="submit" class="btn btn-verde" value="Enviar" />
				</div>
			</form>
		</div>

		<div class="infor-reclutador">
			<h2>Informacion Reclutador</h2>

			{{#if vacante.autor.nombre}}
				<p>{{vacante.autor.nombre}}</p>
			{{/if}}

			{{#if vacante.autor.imagen}}
				<img src="/uploads/perfiles/{{vacante.autor.imagen}}" />
			{{/if}}
		</div>
	</div>	

</body>
</html>